"use client";

import { useMemo, useState, type ChangeEvent, type ReactNode } from "react";
import * as XLSX from "xlsx";

type Tab = "overview" | "rules" | "detect" | "daily" | "summary" | "handover" | "inventory" | "payment";
type PaymentShift = "早班" | "夜班" | "合计" | "未知";
type Rule = { id: string; name: string; keywords: string; includeSales: boolean; includeStockCheck: boolean; includeInventory: boolean };
type ProductRow = { date: string; sheetName: string; rowNumber: number; order: number; category: string; name: string; price: number; purchase: number; morningStock: number; morningSold: number; morningAmount: number; nightStock: number; nightSold: number; nightAmount: number; totalAmount: number; hasMorningStock: boolean; hasNightStock: boolean; hasTotalAmount: boolean; expectedNightStock: number; stockDiff: number; amountDiff: number };
type PaymentRecord = { date: string; sheetName: string; shift: PaymentShift; channel: string; amount: number; rowOrder: number };
type SalesTotalRecord = { date: string; sheetName: string; amount: number; rowOrder: number };
type SheetPreview = { sheetName: string; date: string; rowCount: number; paymentCount: number; salesTotal: number; tableCount: number; skipped?: boolean };
type ProductSummary = { category: string; name: string; price: number; purchase: number; morningSold: number; nightSold: number; totalSold: number; totalAmount: number; endingStock: number; latestDate: string; sourceOrder: number };
type Issue = { type: string; date: string; category: string; name: string; expected: number; actual: number; diff: number; note: string };

const STORE_NAME = "SY11";
const UNKNOWN_CATEGORY = "未分类";
const tabs: Array<[Tab, string]> = [["overview", "总览"], ["rules", "分类规则"], ["detect", "识别预览"], ["daily", "每日报表"], ["summary", "商品总汇"], ["handover", "交接核对"], ["inventory", "自动库存盘点"], ["payment", "收款统计"]];
const DEFAULT_RULES: Rule[] = [
  { id: "cigarette", name: "香烟(酒店库存)", keywords: "烟,香烟,卷烟,烟草,中华,芙蓉王,利群,玉溪,黄鹤楼,云烟,南京,双喜,红塔山,白沙,娇子,黄金叶,苏烟,泰山,七匹狼,中南海,牡丹,贵烟,真龙,钻石,煊赫门,万宝路,黄山,长白山,延安,兰州,宽窄,荷花,天子,红河,红金龙,金圣,人民大会堂,细支,中支,雨花石", includeSales: true, includeStockCheck: true, includeInventory: true },
  { id: "drink", name: "饮料", keywords: "饮料,矿泉水,纯净水,可乐,雪碧,芬达,苏打水,气泡水,红牛,东鹏,脉动,外星人,农夫山泉,怡宝,百岁山,王老吉,加多宝,茶,冰红茶,绿茶,乌龙茶,咖啡,牛奶,酸奶,椰汁,果汁,柠檬茶,阿萨姆,海之言,娃哈哈,康师傅,打火机,火机,火柴,点烟器,方便面,泡面,螺蛳粉,零食,口香糖,杂项,其他,小商品,日用品,百货,纸巾,扑克牌,牙刷,牙膏,剃须,充电器,数据线,雨伞", includeSales: true, includeStockCheck: true, includeInventory: true }
];

function text(value: unknown) { return String(value ?? "").trim(); }
function compact(value: unknown) { return text(value).replace(/\s/g, ""); }
function hasValue(value: unknown) { return value !== null && value !== undefined && text(value) !== ""; }
function toNumber(value: unknown) { if (typeof value === "number") return Number.isFinite(value) ? value : 0; const parsed = Number(text(value).replace(/,/g, "").replace(/[￥¥元\s]/g, "")); return Number.isFinite(parsed) ? parsed : 0; }
function round2(value: number) { return Math.round((value + Number.EPSILON) * 100) / 100; }
function money(value: number) { return round2(value).toFixed(2); }
function safeName(name: string) { return name.replace(/\.xlsx?$/i, "").replace(/[\\/:*?\"<>|]/g, "-"); }
function ruleKeywords(rule: Rule) { return rule.keywords.split(/[,，、\n]/).map((item) => compact(item)).filter(Boolean); }
function ruleByName(rules: Rule[], name: string) { return rules.find((rule) => rule.name === name); }
function categorize(source: string, rules: Rule[]) { const value = compact(source); if (!value) return UNKNOWN_CATEGORY; for (const rule of rules) if (ruleKeywords(rule).some((keyword) => value.includes(keyword))) return rule.name; return UNKNOWN_CATEGORY; }
function isSummaryLikeName(name: string) { return /合计|收款|备注|售卖金额|售卖数量|渠道|小计|总计/.test(name); }
function isAmountLikeLabel(value: string) { return /^(金额|收款金额|合计|总计|早班|白班|夜班|收款渠道|渠道|售卖金额|售卖数量)$/.test(value) || /^\d+(\.\d+)?$/.test(value); }
function cleanChannelName(value: unknown) { const channel = compact(value); return channel && !isAmountLikeLabel(channel) ? channel : ""; }
function hasExplicitFullDate(sheetName: string, title: unknown) { return /(20\d{2}|19\d{2})\s*年\s*\d{1,2}\s*月\s*\d{1,2}\s*日?/.test(`${String(title ?? "")} ${sheetName}`); }
function shouldProcessSheet(sheetName: string, grid: unknown[][]) { return /^\d{1,2}$/.test(sheetName) || hasExplicitFullDate(sheetName, grid[0]?.[0]); }
function extractDate(sheetName: string, title: unknown, fallbackIndex: number) { const raw = `${String(title ?? "")} ${sheetName}`; const full = raw.match(/(20\d{2}|19\d{2})\s*年\s*(\d{1,2})\s*月\s*(\d{1,2})\s*日?/); if (full) return `${full[1]}-${full[2].padStart(2, "0")}-${full[3].padStart(2, "0")}`; const ym = raw.match(/(20\d{2}|19\d{2})\s*年\s*(\d{1,2})\s*月/); const day = /^\d{1,2}$/.test(sheetName) ? Number(sheetName) : fallbackIndex + 1; return `${ym?.[1] ?? new Date().getFullYear()}-${String(ym?.[2] ?? new Date().getMonth() + 1).padStart(2, "0")}-${String(day).padStart(2, "0")}`; }
function makeEmptyTotals(channels: string[]) { return Object.fromEntries(channels.map((channel) => [channel, 0])) as Record<string, number>; }
function productKey(row: { category: string; name: string; price: number }) { return `${row.category}__${row.name}__${row.price}`; }
function rowText(row: unknown[] | undefined) { return (row ?? []).map(text).filter(Boolean).join(" "); }
function inferCategoryFromContext(grid: unknown[][], headerRow: number, startCol: number, endCol: number, sheetName: string, rules: Rule[]) { const samples = [sheetName]; for (let row = 0; row <= headerRow; row += 1) for (let col = startCol; col <= endCol; col += 1) samples.push(text(grid[row]?.[col])); return categorize(samples.join(" "), rules); }
function findTableGroups(grid: unknown[][], sheetName: string, rules: Rule[]) { const groups: Array<{ headerRow: number; startCol: number; nameCol: number; priceCol: number; purchaseCol: number; morningStockCol: number; morningSoldCol: number; morningAmountCol: number; nightStockCol: number; nightSoldCol: number; nightAmountCol: number; totalAmountCol: number; defaultCategory: string }> = []; const seen = new Set<string>(); grid.forEach((row, headerRow) => row.forEach((cell, col) => { if (!/^(商品名称|售卖商品)$/.test(text(cell)) || !/^(价格|售价)$/.test(text(row[col + 1]))) return; const startCol = Math.max(0, col - 1); const key = `${headerRow}_${startCol}`; if (seen.has(key)) return; seen.add(key); groups.push({ headerRow, startCol, nameCol: col, priceCol: col + 1, purchaseCol: col + 2, morningStockCol: col + 3, morningSoldCol: col + 4, morningAmountCol: col + 5, nightStockCol: col + 6, nightSoldCol: col + 7, nightAmountCol: col + 8, totalAmountCol: col + 9, defaultCategory: inferCategoryFromContext(grid, headerRow, startCol, col + 9, sheetName, rules) }); })); return groups; }
function latestByDate(items: ProductRow[]) { const sorted = [...items].sort((a, b) => a.date.localeCompare(b.date) || a.order - b.order); return sorted[sorted.length - 1]; }
function paymentShiftFromContext(grid: unknown[][], rowIndex: number, col: number): PaymentShift { const fromCol = Math.max(0, col - 2); const toCol = col + 8; for (let r = rowIndex - 1; r >= Math.max(0, rowIndex - 8); r -= 1) { const line = (grid[r] ?? []).slice(fromCol, toCol).map(compact).filter(Boolean).join(""); if (!line) continue; if (/(早班|白班)/.test(line)) return "早班"; if (/夜班/.test(line)) return "夜班"; if (/^(合计售卖金额|合计售卖数量|总计售卖金额|总计售卖数量|收款合计|总计|合计)/.test(line)) return "合计"; } return "未知"; }
function parsePaymentRecords(grid: unknown[][], sheetName: string, date: string) { const records: PaymentRecord[] = []; grid.forEach((row, rowIndex) => row.forEach((cell, col) => { if (text(cell) !== "收款渠道") return; const shift = paymentShiftFromContext(grid, rowIndex, col); for (let c = col + 1; c < row.length - 1; c += 2) { const channel = cleanChannelName(row[c]); const amount = round2(toNumber(row[c + 1])); if (channel && amount) records.push({ date, sheetName, shift, channel, amount, rowOrder: rowIndex * 1000 + c }); } })); return records; }
function paymentSource(records: PaymentRecord[]) { const total = records.filter((r) => r.shift === "合计"); if (total.length) return total; return records.filter((r) => r.shift !== "合计"); }
function parseOfficialSalesTotals(grid: unknown[][], sheetName: string, date: string) { const out: SalesTotalRecord[] = []; grid.forEach((row, rowIndex) => row.forEach((cell, col) => { const label = compact(cell); if (label !== "合计售卖金额" && label !== "总计售卖金额") return; for (let c = col + 1; c < Math.min(row.length, col + 8); c += 1) { const amount = toNumber(row[c]); if (amount) { out.push({ date, sheetName, amount, rowOrder: rowIndex * 1000 + c }); break; } } })); return out; }
function parseWorkbook(workbook: XLSX.WorkBook, rules: Rule[]) { const parsedRows: ProductRow[] = []; const payments: PaymentRecord[] = []; const salesTotals: SalesTotalRecord[] = []; const sheets: SheetPreview[] = []; workbook.SheetNames.forEach((sheetName, sheetIndex) => { const sheet = workbook.Sheets[sheetName]; if (!sheet) return; const grid = XLSX.utils.sheet_to_json<unknown[]>(sheet, { header: 1, defval: null }); if (!shouldProcessSheet(sheetName, grid)) { sheets.push({ sheetName, date: "已跳过", rowCount: 0, paymentCount: 0, salesTotal: 0, tableCount: 0, skipped: true }); return; } const date = extractDate(sheetName, grid[0]?.[0], sheetIndex); const sheetPayments = parsePaymentRecords(grid, sheetName, date); const sheetSalesTotals = parseOfficialSalesTotals(grid, sheetName, date); payments.push(...sheetPayments); salesTotals.push(...sheetSalesTotals); let sheetRows = 0; const groups = findTableGroups(grid, sheetName, rules); groups.forEach((group) => { let currentCategory = group.defaultCategory; for (let i = group.headerRow + 1; i < grid.length; i += 1) { const row = grid[i] ?? []; const lineText = rowText(row.slice(group.startCol, group.totalAmountCol + 1)); const lineCategory = categorize(lineText, rules); const name = text(row[group.nameCol]); const price = toNumber(row[group.priceCol]); if (lineCategory !== UNKNOWN_CATEGORY && (!name || !price || /分类|类别|品类|小计|合计|库存|销售|报表/.test(lineText))) currentCategory = lineCategory; if (!name || !price || isSummaryLikeName(name)) continue; const purchaseRaw = row[group.purchaseCol]; const morningStockRaw = row[group.morningStockCol]; const morningSoldRaw = row[group.morningSoldCol]; const nightStockRaw = row[group.nightStockCol]; const nightSoldRaw = row[group.nightSoldCol]; const morningAmountRaw = row[group.morningAmountCol]; const nightAmountRaw = row[group.nightAmountCol]; const totalAmountRaw = row[group.totalAmountCol]; const purchase = toNumber(purchaseRaw); const morningStock = toNumber(morningStockRaw); const morningSold = toNumber(morningSoldRaw); const hasMorningStock = hasValue(morningStockRaw); const hasNightStock = hasValue(nightStockRaw); const hasTotalAmount = hasValue(totalAmountRaw); const nightStock = toNumber(nightStockRaw); const nightSold = toNumber(nightSoldRaw); const morningAmount = toNumber(morningAmountRaw) || round2(morningSold * price); const nightAmount = toNumber(nightAmountRaw) || round2(nightSold * price); const totalAmount = toNumber(totalAmountRaw) || round2(morningAmount + nightAmount); const active = hasValue(purchaseRaw) || hasMorningStock || hasValue(morningSoldRaw) || hasNightStock || hasValue(nightSoldRaw) || hasValue(morningAmountRaw) || hasValue(nightAmountRaw) || hasTotalAmount; if (!active) continue; const expectedNightStock = round2(morningStock + purchase - morningSold); const amountDiff = hasTotalAmount ? round2(totalAmount - morningAmount - nightAmount) : 0; const rawCategory = categorize(`${name} ${currentCategory}`, rules) === UNKNOWN_CATEGORY ? currentCategory : categorize(`${name} ${currentCategory}`, rules); parsedRows.push({ date, sheetName, rowNumber: i + 1, order: sheetIndex * 1_000_000 + group.startCol * 10_000 + i, category: rawCategory, name, price, purchase, morningStock, morningSold, morningAmount, nightStock, nightSold, nightAmount, totalAmount, hasMorningStock, hasNightStock, hasTotalAmount, expectedNightStock, stockDiff: hasNightStock ? round2(nightStock - expectedNightStock) : 0, amountDiff }); sheetRows += 1; } }); sheets.push({ sheetName, date, rowCount: sheetRows, paymentCount: sheetPayments.length, salesTotal: round2(sheetSalesTotals.reduce((s, r) => s + r.amount, 0)), tableCount: groups.length }); }); return { rows: parsedRows.sort((a, b) => a.date.localeCompare(b.date) || a.order - b.order), payments, salesTotals, sheets }; }
function ledgerEndingStock(items: ProductRow[]) { let stock = 0; let hasState = false; [...items].sort((a, b) => a.date.localeCompare(b.date) || a.order - b.order).forEach((row) => { if (row.hasMorningStock) { stock = row.morningStock; hasState = true; } else if (!hasState) { stock = 0; hasState = true; } stock = round2(stock + row.purchase - row.morningSold); if (row.hasNightStock) stock = row.nightStock; stock = round2(stock - row.nightSold); }); return round2(stock); }
function hasRowIssue(row: ProductRow) { return (row.hasNightStock && Math.abs(row.stockDiff) > 0.01) || (row.hasTotalAmount && Math.abs(row.amountDiff) > 0.01); }
function downloadWorkbook(fileName: string, sheets: Record<string, Record<string, unknown>[]>) { const workbook = XLSX.utils.book_new(); Object.entries(sheets).forEach(([name, data]) => XLSX.utils.book_append_sheet(workbook, XLSX.utils.json_to_sheet(data), name.slice(0, 31))); XLSX.writeFile(workbook, fileName); }
function Card({ title, value, desc, tone = "default" }: { title: string; value: string; desc?: string; tone?: "default" | "danger" }) { const cls = tone === "danger" ? "border-red-500 bg-red-600 text-white shadow-sm" : "border bg-white text-slate-900 shadow-sm"; return <div className={`rounded-2xl p-5 ${cls}`}><div className={`text-sm ${tone === "danger" ? "text-red-100" : "text-slate-500"}`}>{title}</div><div className="mt-2 text-2xl font-bold">{value}</div>{desc ? <div className={`mt-1 text-xs ${tone === "danger" ? "text-red-100" : "text-slate-500"}`}>{desc}</div> : null}</div>; }
function Badge({ children, tone = "gray" }: { children: ReactNode; tone?: "gray" | "green" | "red" | "yellow" }) { const cls = { gray: "bg-slate-100 text-slate-700", green: "bg-green-100 text-green-700", red: "bg-red-100 text-red-700", yellow: "bg-yellow-100 text-yellow-800" }[tone]; return <span className={`rounded-full px-2 py-1 text-xs font-medium ${cls}`}>{children}</span>; }
function Table({ rows, headers, emptyText = "暂无数据" }: { rows: Record<string, ReactNode>[]; headers: string[]; emptyText?: string }) { if (!rows.length) return <p className="mt-3 rounded-xl bg-slate-50 p-4 text-sm text-slate-500">{emptyText}</p>; return <div className="table-scroll mt-4 max-h-[560px] overflow-auto rounded-xl border"><table className="min-w-full text-sm"><thead className="sticky top-0 bg-slate-50 text-left"><tr>{headers.map((h) => <th key={h} className="p-3 font-semibold text-slate-700">{h}</th>)}</tr></thead><tbody>{rows.map((row, i) => <tr key={i} className="border-t">{headers.map((h) => <td key={h} className="p-3 whitespace-nowrap">{row[h]}</td>)}</tr>)}</tbody></table></div>; }
function Panel({ title, children }: { title: string; children: ReactNode }) { return <section className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="mb-3 text-lg font-bold text-slate-900">{title}</h2>{children}</section>; }
export default function InventoryPageConfigurable() { const [tab, setTab] = useState<Tab>("overview"); const [rules, setRules] = useState<Rule[]>(DEFAULT_RULES); const [rawRows, setRawRows] = useState<ProductRow[]>([]); const [paymentRecords, setPaymentRecords] = useState<PaymentRecord[]>([]); const [salesTotalRecords, setSalesTotalRecords] = useState<SalesTotalRecord[]>([]); const [sheetPreview, setSheetPreview] = useState<SheetPreview[]>([]); const [fileName, setFileName] = useState(""); const rows = useMemo<ProductRow[]>(() => rawRows.map((row) => ({ ...row, category: categorize(`${row.name} ${row.category}`, rules) })), [rawRows, rules]); async function handleFile(file: File) { const workbook = XLSX.read(await file.arrayBuffer(), { type: "array", cellDates: true }); const parsed = parseWorkbook(workbook, rules); setRawRows(parsed.rows); setPaymentRecords(parsed.payments); setSalesTotalRecords(parsed.salesTotals); setSheetPreview(parsed.sheets); setFileName(file.name); } const salesRows = useMemo(() => rows.filter((row) => ruleByName(rules, row.category)?.includeSales ?? true), [rows, rules]); const stockCheckRows = useMemo(() => rows.filter((row) => ruleByName(rules, row.category)?.includeStockCheck ?? false), [rows, rules]); const inventoryCategoryNames = useMemo(() => new Set(rules.filter((rule) => rule.includeInventory).map((rule) => rule.name)), [rules]); const paymentChannels = useMemo(() => { const channels: string[] = []; paymentRecords.forEach((record) => { if (record.channel && !channels.includes(record.channel)) channels.push(record.channel); }); return channels; }, [paymentRecords]); const paymentDaily = useMemo(() => { const byDate = new Map<string, PaymentRecord[]>(); paymentRecords.forEach((record) => byDate.set(record.date, [...(byDate.get(record.date) ?? []), record])); return Array.from(byDate.entries()).map(([date, records]) => { const source = paymentSource(records); const totals = makeEmptyTotals(paymentChannels); source.forEach((record) => totals[record.channel] = round2((totals[record.channel] ?? 0) + record.amount)); return { date, totals, total: round2(paymentChannels.reduce((sum, channel) => sum + (totals[channel] ?? 0), 0)) }; }).sort((a, b) => a.date.localeCompare(b.date)); }, [paymentRecords, paymentChannels]); const officialSalesDaily = useMemo(() => { const byDate = new Map<string, number>(); salesTotalRecords.forEach((record) => byDate.set(record.date, round2((byDate.get(record.date) ?? 0) + record.amount))); return byDate; }, [salesTotalRecords]); const productDaily = useMemo(() => { const map = new Map<string, { date: string; purchase: number; morningSold: number; nightSold: number; totalSold: number; morningAmount: number; nightAmount: number; totalAmount: number; abnormalCount: number }>(); salesRows.forEach((row) => { const item = map.get(row.date) ?? { date: row.date, purchase: 0, morningSold: 0, nightSold: 0, totalSold: 0, morningAmount: 0, nightAmount: 0, totalAmount: 0, abnormalCount: 0 }; item.purchase += row.purchase; item.morningSold += row.morningSold; item.nightSold += row.nightSold; item.totalSold += row.morningSold + row.nightSold; item.morningAmount += row.morningAmount; item.nightAmount += row.nightAmount; item.totalAmount += row.totalAmount; item.abnormalCount += hasRowIssue(row) ? 1 : 0; map.set(row.date, item); }); return Array.from(map.values()).map((item) => ({ ...item, purchase: round2(item.purchase), morningSold: round2(item.morningSold), nightSold: round2(item.nightSold), totalSold: round2(item.totalSold), morningAmount: round2(item.morningAmount), nightAmount: round2(item.nightAmount), totalAmount: round2(item.totalAmount) })).sort((a, b) => a.date.localeCompare(b.date)); }, [salesRows]); const dailyReconcile = useMemo(() => productDaily.map((item) => { const sales = officialSalesDaily.get(item.date) ?? item.totalAmount; const pay = paymentDaily.find((p) => p.date === item.date)?.total ?? 0; return { date: item.date, productSales: item.totalAmount, officialSales: sales, payment: pay, diff: round2(pay - sales) }; }), [productDaily, paymentDaily, officialSalesDaily]); const productSummary = useMemo<ProductSummary[]>(() => { const map = new Map<string, ProductRow[]>(); salesRows.forEach((row) => map.set(productKey(row), [...(map.get(productKey(row)) ?? []), row])); return Array.from(map.values()).map((items) => { const latest = latestByDate(items); return { category: latest.category, name: latest.name, price: latest.price, purchase: round2(items.reduce((s, r) => s + r.purchase, 0)), morningSold: round2(items.reduce((s, r) => s + r.morningSold, 0)), nightSold: round2(items.reduce((s, r) => s + r.nightSold, 0)), totalSold: round2(items.reduce((s, r) => s + r.morningSold + r.nightSold, 0)), totalAmount: round2(items.reduce((s, r) => s + r.totalAmount, 0)), endingStock: ledgerEndingStock(items), latestDate: latest.date, sourceOrder: Math.min(...items.map((r) => r.order)) }; }).sort((a, b) => a.category.localeCompare(b.category) || a.sourceOrder - b.sourceOrder); }, [salesRows]); const inventoryRows = useMemo(() => productSummary.filter((item) => inventoryCategoryNames.has(item.category)), [productSummary, inventoryCategoryNames]); const categorySummary = useMemo(() => { const map = new Map<string, { category: string; totalSold: number; totalAmount: number; count: number; sourceOrder: number }>(); productSummary.forEach((item) => { const current = map.get(item.category) ?? { category: item.category, totalSold: 0, totalAmount: 0, count: 0, sourceOrder: item.sourceOrder }; current.totalSold += item.totalSold; current.totalAmount += item.totalAmount; current.count += 1; current.sourceOrder = Math.min(current.sourceOrder, item.sourceOrder); map.set(item.category, current); }); return Array.from(map.values()).map((item) => ({ ...item, totalSold: round2(item.totalSold), totalAmount: round2(item.totalAmount) })).sort((a, b) => a.sourceOrder - b.sourceOrder); }, [productSummary]); const issues = useMemo(() => stockCheckRows.flatMap((row) => { const out: Issue[] = []; if (row.hasNightStock && Math.abs(row.stockDiff) > 0.01) out.push({ type: "早班交夜班", date: row.date, category: row.category, name: row.name, expected: row.expectedNightStock, actual: row.nightStock, diff: row.stockDiff, note: "夜班库存应等于：早班库存 + 进货 - 早班售卖数量。" }); if (row.hasTotalAmount && Math.abs(row.amountDiff) > 0.01) out.push({ type: "金额核对", date: row.date, category: row.category, name: row.name, expected: round2(row.morningAmount + row.nightAmount), actual: row.totalAmount, diff: row.amountDiff, note: "总金额应等于早班金额 + 夜班金额。" }); return out; }), [stockCheckRows]); const totalSold = productDaily.reduce((sum, item) => sum + item.totalSold, 0); const totalAmount = dailyReconcile.reduce((sum, item) => sum + item.officialSales, 0); const totalPaymentAmount = paymentDaily.reduce((sum, item) => sum + item.total, 0); const paymentDiff = round2(totalPaymentAmount - totalAmount); const paymentSummary = paymentChannels.map((channel) => ({ channel, amount: round2(paymentDaily.reduce((sum, day) => sum + (day.totals[channel] ?? 0), 0)) })); function updateRule(id: string, patch: Partial<Rule>) { setRules((old) => old.map((rule) => rule.id === id ? { ...rule, ...patch } : rule)); } function addRule() { setRules((old) => [...old, { id: `rule_${Date.now()}`, name: "新分类", keywords: "", includeSales: true, includeStockCheck: false, includeInventory: true }]); } function removeRule(id: string) { setRules((old) => old.filter((rule) => rule.id !== id)); } function exportSalesSummaryReport() { if (!productSummary.length) return alert("请先上传原始报表 Excel"); downloadWorkbook(`${STORE_NAME}-销售汇总报表.xlsx`, { 销售汇总报表: productSummary.map((item, index) => ({ 序号: index + 1, 商品分类: item.category, 商品名称: item.name, 平均价格: item.price, 数量: item.totalSold, 金额: item.totalAmount })), 分类汇总: categorySummary.map((item, index) => ({ 序号: index + 1, 商品分类: item.category, 品规数: item.count, 销量: item.totalSold, 销售金额: item.totalAmount })), 收款渠道月统计: paymentSummary.map((item, index) => ({ 序号: index + 1, 收款渠道: item.channel, 月收入: item.amount })) }); } function exportExceptionReport() { if (!issues.length) return alert("当前没有异常。"); downloadWorkbook("报表异常核对报表.xlsx", { 异常核对报表: issues.map((item, index) => ({ 序号: index + 1, 异常类型: item.type, 商品分类: item.category, 日期: item.date, 商品名称: item.name, 应为: item.expected, 实际: item.actual, 差异: item.diff, 说明: item.note })) }); } function exportAutoInventoryReport() { if (!inventoryRows.length) return alert("当前没有可自动盘点的商品。请在分类规则里勾选自动盘点。"); downloadWorkbook("自动库存盘点表.xlsx", { 自动库存盘点: inventoryRows.map((item, index) => ({ 序号: index + 1, 分类: item.category, 商品名称: item.name, 价格: item.price, 本月进货: item.purchase, 本月总销量: item.totalSold, 月末账面应剩: item.endingStock, 最新日期: item.latestDate })) }); } return <main className="min-h-screen bg-slate-50 p-4 md:p-8"><div className="mx-auto max-w-7xl"><header className="rounded-3xl bg-slate-950 p-6 text-white shadow-sm"><p className="text-sm text-slate-300">{STORE_NAME} 商品经营详细核对</p><h1 className="mt-1 text-2xl font-bold md:text-4xl">商品日报智能核对系统</h1><p className="mt-2 text-sm text-slate-300">已修复：收款差异使用表内合计售卖金额；自动盘点按商品逐日滚动计算。</p><div className="mt-5 flex flex-wrap gap-3"><label className="inline-flex cursor-pointer items-center rounded-xl bg-white px-4 py-3 text-sm font-medium text-slate-950"><input className="hidden" type="file" accept=".xlsx,.xls" onChange={(event: ChangeEvent<HTMLInputElement>) => event.target.files?.[0] && handleFile(event.target.files[0])} />导入商品报表 Excel</label>{fileName ? <span className="rounded-xl bg-slate-800 px-4 py-3 text-sm text-slate-200">当前文件：{fileName}</span> : null}</div></header><nav className="my-5 flex flex-wrap gap-2">{tabs.map(([key, label]) => <button key={key} onClick={() => setTab(key)} className={`rounded-xl px-4 py-2 text-sm font-medium ${tab === key ? "bg-slate-950 text-white" : "bg-white text-slate-700 shadow-sm hover:bg-slate-100"}`}>{label}</button>)}</nav><section className="mb-5 grid gap-4 md:grid-cols-4"><Card title="销售金额" value={`¥${money(totalAmount)}`} desc={`${totalSold} 件`} /><Card title="渠道收款" value={`¥${money(totalPaymentAmount)}`} desc="有总合计只用总合计" /><Card title="收款差异" value={`¥${money(paymentDiff)}`} desc="收款 - 表内合计售卖金额" tone={Math.abs(paymentDiff) > 0.01 ? "danger" : "default"} /><Card title="异常数" value={String(issues.length)} desc="库存/金额异常" tone={issues.length ? "danger" : "default"} /></section>{tab === "overview" ? <Panel title="总览"><div className="grid gap-4 md:grid-cols-3"><Card title="识别商品行" value={String(rows.length)} /><Card title="商品品规" value={String(productSummary.length)} /><Card title="工作表" value={String(sheetPreview.length)} /></div></Panel> : null}{tab === "rules" ? <Panel title="分类规则"><button onClick={addRule} className="mb-3 rounded-xl bg-slate-950 px-4 py-2 text-sm font-medium text-white">新增分类</button><div className="space-y-3">{rules.map((rule) => <div key={rule.id} className="rounded-xl border bg-slate-50 p-4"><div className="grid gap-3 md:grid-cols-4"><input value={rule.name} onChange={(e) => updateRule(rule.id, { name: e.target.value })} className="rounded-lg border p-2" /><input value={rule.keywords} onChange={(e) => updateRule(rule.id, { keywords: e.target.value })} className="rounded-lg border p-2 md:col-span-2" /><button onClick={() => removeRule(rule.id)} className="rounded-lg bg-red-600 px-3 py-2 text-sm text-white">删除</button></div><div className="mt-3 flex flex-wrap gap-4 text-sm"><label><input type="checkbox" checked={rule.includeSales} onChange={(e) => updateRule(rule.id, { includeSales: e.target.checked })} /> 参与销售</label><label><input type="checkbox" checked={rule.includeStockCheck} onChange={(e) => updateRule(rule.id, { includeStockCheck: e.target.checked })} /> 库存核对</label><label><input type="checkbox" checked={rule.includeInventory} onChange={(e) => updateRule(rule.id, { includeInventory: e.target.checked })} /> 自动盘点</label></div></div>)}</div></Panel> : null}{tab === "detect" ? <Panel title="识别预览"><Table headers={["Sheet", "日期", "商品行", "收款记录", "表内合计售卖金额", "商品表块", "状态"]} rows={sheetPreview.map((item) => ({ Sheet: item.sheetName, 日期: item.date, 商品行: item.rowCount, 收款记录: item.paymentCount, 表内合计售卖金额: money(item.salesTotal), 商品表块: item.tableCount, 状态: item.skipped ? "已跳过备用表" : "已解析" }))} /></Panel> : null}{tab === "daily" ? <Panel title="每日报表"><Table headers={["日期", "进货", "早班销量", "夜班销量", "总销量", "商品明细金额", "表内合计售卖金额", "异常"]} rows={productDaily.map((item) => ({ 日期: item.date, 进货: item.purchase, 早班销量: item.morningSold, 夜班销量: item.nightSold, 总销量: item.totalSold, 商品明细金额: money(item.totalAmount), 表内合计售卖金额: money(officialSalesDaily.get(item.date) ?? item.totalAmount), 异常: item.abnormalCount }))} /></Panel> : null}{tab === "summary" ? <Panel title="商品总汇"><button onClick={exportSalesSummaryReport} className="rounded-xl bg-green-700 px-4 py-2 text-sm font-medium text-white">导出销售汇总</button><Table headers={["分类", "商品", "价格", "进货", "早班销量", "夜班销量", "总销量", "销售金额", "月末库存"]} rows={productSummary.map((item) => ({ 分类: item.category, 商品: item.name, 价格: item.price, 进货: item.purchase, 早班销量: item.morningSold, 夜班销量: item.nightSold, 总销量: item.totalSold, 销售金额: money(item.totalAmount), 月末库存: item.endingStock }))} /></Panel> : null}{tab === "handover" ? <Panel title="交接核对"><button onClick={exportExceptionReport} className="rounded-xl bg-red-700 px-4 py-2 text-sm font-medium text-white">导出异常报表</button><Table headers={["类型", "日期", "分类", "商品", "应为", "实际", "差异", "说明"]} rows={issues.map((item) => ({ 类型: <Badge tone={item.type === "金额核对" ? "yellow" : "red"}>{item.type}</Badge>, 日期: item.date, 分类: item.category, 商品: item.name, 应为: item.expected, 实际: item.actual, 差异: item.diff, 说明: item.note }))} emptyText="暂无异常" /></Panel> : null}{tab === "inventory" ? <Panel title="自动库存盘点"><button onClick={exportAutoInventoryReport} className="rounded-xl bg-green-700 px-4 py-2 text-sm font-medium text-white">导出自动库存盘点</button><Table headers={["分类", "商品", "价格", "进货", "销量", "月末账面应剩", "最新日期"]} rows={inventoryRows.map((item) => ({ 分类: item.category, 商品: item.name, 价格: item.price, 进货: item.purchase, 销量: item.totalSold, 月末账面应剩: item.endingStock, 最新日期: item.latestDate }))} /></Panel> : null}{tab === "payment" ? <Panel title="收款统计"><p className="mb-3 text-sm text-slate-500">核对规则：当天有总合计收款时，只用总合计；销售核对金额优先使用表内“合计售卖金额”。</p><Table headers={["日期", "商品明细金额", "表内合计售卖金额", "有效收款", "差异"]} rows={dailyReconcile.map((item) => ({ 日期: item.date, 商品明细金额: money(item.productSales), 表内合计售卖金额: money(item.officialSales), 有效收款: money(item.payment), 差异: money(item.diff) }))} /><Table headers={["收款渠道", "月收入"]} rows={paymentSummary.map((item) => ({ 收款渠道: item.channel, 月收入: money(item.amount) }))} /><Table headers={["日期", ...paymentChannels, "当日合计"]} rows={paymentDaily.map((item) => Object.fromEntries([["日期", item.date], ...paymentChannels.map((channel) => [channel, money(item.totals[channel] ?? 0)]), ["当日合计", money(item.total)]]))} /></Panel> : null}</div></main>; }
