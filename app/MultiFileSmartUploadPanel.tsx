"use client";

import { useState, type ChangeEvent } from "react";
import * as XLSX from "xlsx";

type BatchKind = "product" | "payroll" | "generic";
type PaymentShift = "早班" | "夜班" | "合计" | "未知";
type BatchIssue = { fileName: string; storeName: string; type: string; message: string };
type PaymentRecord = { fileName: string; storeName: string; date: string; sheetName: string; shift: PaymentShift; channel: string; amount: number; order: number };
type BatchResult = { fileName: string; storeName: string; kind: BatchKind; rowCount: number; primaryAmount: number; secondaryAmount: number; diffAmount: number; issueCount: number; channelCount: number; issues: BatchIssue[]; payments: PaymentRecord[] };

function text(value: unknown) { return String(value ?? "").trim(); }
function compact(value: unknown) { return text(value).replace(/\s/g, ""); }
function toNumber(value: unknown) { if (typeof value === "number") return Number.isFinite(value) ? value : 0; const parsed = Number(text(value).replace(/,/g, "").replace(/[￥¥元%\s]/g, "")); return Number.isFinite(parsed) ? parsed : 0; }
function round2(value: number) { return Math.round((value + Number.EPSILON) * 100) / 100; }
function money(value: number) { return round2(value).toFixed(2); }
function storeFromFileName(fileName: string) { return fileName.replace(/\.(xlsx|xls)$/i, "").replace(/\d{4}[-年.]?\d{1,2}[-月.]?\d{0,2}[日]?/g, "").replace(/报表|日报|月报|工资表|库存表|核对表/g, "").replace(/[-_ ]+/g, " ").trim() || fileName.replace(/\.(xlsx|xls)$/i, ""); }
function cleanChannelName(value: unknown) { const channel = compact(value); if (!channel) return ""; if (/^(金额|收款金额|合计|总计|早班|白班|夜班|收款渠道|渠道|售卖金额)$/.test(channel)) return ""; if (/^\d+(\.\d+)?$/.test(channel)) return ""; return channel; }
function hasExplicitFullDate(sheetName: string, title: unknown) { return /(20\d{2}|19\d{2})\s*年\s*\d{1,2}\s*月\s*\d{1,2}\s*日?/.test(`${String(title ?? "")} ${sheetName}`); }
function shouldProcessSheet(sheetName: string, grid: unknown[][]) { return /^\d{1,2}$/.test(sheetName) || hasExplicitFullDate(sheetName, grid[0]?.[0]); }
function extractDate(sheetName: string, title: unknown, fallbackIndex: number) { const raw = `${String(title ?? "")} ${sheetName}`; const full = raw.match(/(20\d{2}|19\d{2})\s*年\s*(\d{1,2})\s*月\s*(\d{1,2})\s*日?/); if (full) return `${full[1]}-${full[2].padStart(2, "0")}-${full[3].padStart(2, "0")}`; const ym = raw.match(/(20\d{2}|19\d{2})\s*年\s*(\d{1,2})\s*月/); const day = sheetName.match(/^(\d{1,2})$/) ? Number(sheetName) : fallbackIndex + 1; return `${ym?.[1] ?? new Date().getFullYear()}-${String(ym?.[2] ?? new Date().getMonth() + 1).padStart(2, "0")}-${String(day).padStart(2, "0")}`; }
function cellWindow(grid: unknown[][], rowIndex: number, col: number) { const parts: string[] = []; const fromCol = Math.max(0, col - 2); const toCol = col + 8; for (let r = rowIndex; r >= Math.max(0, rowIndex - 8); r -= 1) parts.push(...(grid[r] ?? []).slice(fromCol, toCol).map(compact).filter(Boolean)); return parts.join(""); }
function paymentShiftFromContext(grid: unknown[][], rowIndex: number, col: number): PaymentShift { const line = cellWindow(grid, rowIndex, col); if (/(早班|白班)/.test(line)) return "早班"; if (/夜班/.test(line)) return "夜班"; if (/(^|[^早白夜])合计售卖(金额|数量)|(^|[^早白夜])总计/.test(line)) return "合计"; return "未知"; }
function parsePaymentRecords(grid: unknown[][], sheetName: string, date: string, fileName: string, storeName: string) { const records: PaymentRecord[] = []; grid.forEach((row, rowIndex) => row.forEach((cell, col) => { if (text(cell) !== "收款渠道") return; const shift = paymentShiftFromContext(grid, rowIndex, col); for (let c = col + 1; c < row.length - 1; c += 2) { const channel = cleanChannelName(row[c]); const amount = round2(toNumber(row[c + 1])); if (channel && amount) records.push({ fileName, storeName, date, sheetName, shift, channel, amount, order: rowIndex * 1000 + c }); } })); return records; }
function paymentSourceForReconcile(payments: PaymentRecord[]) { const byFileDate = new Map<string, PaymentRecord[]>(); payments.forEach((record) => byFileDate.set(`${record.fileName}__${record.date}`, [...(byFileDate.get(`${record.fileName}__${record.date}`) ?? []), record])); return Array.from(byFileDate.values()).flatMap((records) => { const totalRecords = records.filter((record) => record.shift === "合计"); if (totalRecords.length) return totalRecords; return records.filter((record) => record.shift !== "合计"); }); }
function channelSummary(payments: PaymentRecord[]) { const map = new Map<string, { channel: string; amount: number; count: number }>(); paymentSourceForReconcile(payments).forEach((record) => { const old = map.get(record.channel) ?? { channel: record.channel, amount: 0, count: 0 }; old.amount = round2(old.amount + record.amount); old.count += 1; map.set(record.channel, old); }); return Array.from(map.values()).sort((a, b) => b.amount - a.amount); }
function findProductGroups(grid: unknown[][]) { const groups: Array<{ headerRow: number; nameCol: number; priceCol: number; purchaseCol: number; morningStockCol: number; morningSoldCol: number; morningAmountCol: number; nightStockCol: number; nightSoldCol: number; nightAmountCol: number; totalAmountCol: number }> = []; grid.forEach((row, headerRow) => row.forEach((cell, col) => { const label = compact(cell); const next = compact(row[col + 1]); if ((label === "商品名称" || label === "售卖商品") && (next === "价格" || next === "售价")) groups.push({ headerRow, nameCol: col, priceCol: col + 1, purchaseCol: col + 2, morningStockCol: col + 3, morningSoldCol: col + 4, morningAmountCol: col + 5, nightStockCol: col + 6, nightSoldCol: col + 7, nightAmountCol: col + 8, totalAmountCol: col + 9 }); })); return groups; }
function result(fileName: string, storeName: string, kind: BatchKind, rowCount: number, primaryAmount: number, secondaryAmount: number, diffAmount: number, issues: BatchIssue[], payments: PaymentRecord[] = []): BatchResult { return { fileName, storeName, kind, rowCount, primaryAmount: round2(primaryAmount), secondaryAmount: round2(secondaryAmount), diffAmount: round2(diffAmount), issueCount: issues.length, channelCount: channelSummary(payments).length, issues, payments }; }
function parseProductFile(workbook: XLSX.WorkBook, fileName: string, storeName: string): BatchResult | null { let rowCount = 0; let salesAmount = 0; const issues: BatchIssue[] = []; const payments: PaymentRecord[] = []; workbook.SheetNames.forEach((sheetName, sheetIndex) => { const sheet = workbook.Sheets[sheetName]; if (!sheet) return; const grid = XLSX.utils.sheet_to_json<unknown[]>(sheet, { header: 1, defval: null }); if (!shouldProcessSheet(sheetName, grid)) return; const date = extractDate(sheetName, grid[0]?.[0], sheetIndex); payments.push(...parsePaymentRecords(grid, sheetName, date, fileName, storeName)); findProductGroups(grid).forEach((group) => { for (let r = group.headerRow + 1; r < grid.length; r += 1) { const line = grid[r] ?? []; const name = text(line[group.nameCol]); const price = toNumber(line[group.priceCol]); if (!name || !price || /合计|总计|小计|备注|收款|金额|售卖数量|售卖金额|渠道/.test(name)) continue; const purchase = toNumber(line[group.purchaseCol]); const morningStock = toNumber(line[group.morningStockCol]); const morningSold = toNumber(line[group.morningSoldCol]); const morningAmount = toNumber(line[group.morningAmountCol]) || round2(morningSold * price); const nightStockRaw = line[group.nightStockCol]; const nightStock = toNumber(nightStockRaw); const nightSold = toNumber(line[group.nightSoldCol]); const nightAmount = toNumber(line[group.nightAmountCol]) || round2(nightSold * price); const totalAmount = toNumber(line[group.totalAmountCol]) || round2(morningAmount + nightAmount); if (!(purchase || morningStock || morningSold || nightStock || nightSold || totalAmount)) continue; rowCount += 1; salesAmount += totalAmount; const expectedNightStock = round2(morningStock + purchase - morningSold); if (nightStockRaw !== null && nightStockRaw !== undefined && text(nightStockRaw) !== "" && Math.abs(nightStock - expectedNightStock) > 0.01) issues.push({ fileName, storeName, type: "商品交接异常", message: `${sheetName} 第${r + 1}行 ${name}：夜班库存应为 ${expectedNightStock}，实际 ${nightStock}` }); } }); }); if (rowCount < 3) return null; const paymentAmount = round2(paymentSourceForReconcile(payments).reduce((sum, record) => sum + record.amount, 0)); return result(fileName, storeName, "product", rowCount, salesAmount, paymentAmount, paymentAmount - salesAmount, issues, payments); }
function parseGenericFile(workbook: XLSX.WorkBook, fileName: string, storeName: string): BatchResult { return result(fileName, storeName, "generic", 0, 0, 0, 0, []); }
async function analyzeFile(file: File): Promise<BatchResult> { const workbook = XLSX.read(await file.arrayBuffer(), { type: "array", cellDates: true }); const fileName = file.name; const storeName = storeFromFileName(fileName); return parseProductFile(workbook, fileName, storeName) ?? parseGenericFile(workbook, fileName, storeName); }
function exportBatch(results: BatchResult[]) { const wb = XLSX.utils.book_new(); XLSX.utils.book_append_sheet(wb, XLSX.utils.json_to_sheet(results.map((item, index) => ({ 序号: index + 1, 门店: item.storeName, 文件: item.fileName, 类型: item.kind === "product" ? "商品表" : item.kind === "payroll" ? "工资表" : "通用表", 数据行: item.rowCount, 销售或应发: item.primaryAmount, 收款或实发: item.secondaryAmount, 核对差异: item.diffAmount, 渠道数: item.channelCount, 异常数: item.issueCount }))), "全部门店总览"); XLSX.utils.book_append_sheet(wb, XLSX.utils.json_to_sheet(results.flatMap((item) => paymentSourceForReconcile(item.payments)).map((record, index) => ({ 序号: index + 1, 门店: record.storeName, 文件: record.fileName, 日期: record.date, Sheet: record.sheetName, 班次: record.shift, 收款渠道: record.channel, 金额: record.amount }))), "有效收款明细"); XLSX.writeFile(wb, `智能核对报告-${new Date().toISOString().slice(0, 10)}.xlsx`); }
export default function MultiFileSmartUploadPanel() { const [results, setResults] = useState<BatchResult[]>([]); const [loading, setLoading] = useState(false); async function handleFiles(event: ChangeEvent<HTMLInputElement>) { const files = Array.from(event.target.files ?? []).filter((file) => /\.(xlsx|xls)$/i.test(file.name)); if (!files.length) return; setLoading(true); try { setResults(await Promise.all(files.map(analyzeFile))); } finally { setLoading(false); event.target.value = ""; } } const totalPrimary = results.reduce((sum, item) => sum + item.primaryAmount, 0); const totalSecondary = results.reduce((sum, item) => sum + item.secondaryAmount, 0); const totalDiff = results.reduce((sum, item) => sum + item.diffAmount, 0); const totalRows = results.reduce((sum, item) => sum + item.rowCount, 0); const totalIssues = results.reduce((sum, item) => sum + item.issueCount, 0); const validChannels = channelSummary(results.flatMap((item) => item.payments)); return <section className="mx-auto mb-6 max-w-7xl px-4 md:px-8"><div className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex flex-col gap-4 md:flex-row md:items-center md:justify-between"><div><h2 className="text-lg font-bold text-slate-900">统一自动识别上传</h2><p className="mt-1 text-sm text-slate-500">已修复：按收款渠道所在列识别白班、夜班、总合计。</p></div><label className="inline-flex cursor-pointer items-center justify-center rounded-xl bg-slate-950 px-4 py-3 text-sm font-medium text-white">{loading ? "处理中..." : "选择 Excel"}<input className="hidden" type="file" accept=".xlsx,.xls" multiple onChange={handleFiles} /></label></div>{results.length ? <><div className="mt-5 grid gap-4 md:grid-cols-5"><Card label="文件数" value={String(results.length)} /><Card label="数据行" value={String(totalRows)} /><Card label="销售/应发" value={`¥${money(totalPrimary)}`} /><Card label="收款/实发" value={`¥${money(totalSecondary)}`} /><Card label="核对差异" value={`¥${money(totalDiff)}`} danger={Math.abs(totalDiff) > 0.01 || totalIssues > 0} /></div><div className="mt-5 overflow-auto rounded-xl border"><table className="min-w-full text-sm"><thead className="bg-slate-50 text-left"><tr>{["门店", "文件", "类型", "数据行", "销售/应发", "收款/实发", "核对差异", "渠道数", "异常"].map((h) => <th key={h} className="p-3">{h}</th>)}</tr></thead><tbody>{results.map((item) => <tr key={item.fileName} className="border-t"><td className="p-3">{item.storeName}</td><td className="p-3">{item.fileName}</td><td className="p-3">{item.kind === "product" ? "商品表" : item.kind === "payroll" ? "工资表" : "通用表"}</td><td className="p-3">{item.rowCount}</td><td className="p-3">¥{money(item.primaryAmount)}</td><td className="p-3">¥{money(item.secondaryAmount)}</td><td className="p-3">¥{money(item.diffAmount)}</td><td className="p-3">{item.channelCount}</td><td className="p-3">{item.issueCount}</td></tr>)}</tbody></table></div><div className="mt-5 rounded-xl bg-slate-50 p-4"><div className="font-bold text-slate-900">有效收款渠道汇总</div><div className="mt-3 grid gap-2 md:grid-cols-3">{validChannels.map((item) => <div key={item.channel} className="rounded-xl bg-white p-3 text-sm shadow-sm"><div className="font-medium">{item.channel}</div><div className="mt-1 text-lg font-bold">¥{money(item.amount)}</div><div className="text-xs text-slate-500">{item.count} 条有效记录</div></div>)}</div></div><button onClick={() => exportBatch(results)} className="mt-5 rounded-xl bg-green-700 px-4 py-3 text-sm font-medium text-white">导出总报告</button></> : null}</div></section>; }
function Card({ label, value, danger = false }: { label: string; value: string; danger?: boolean }) { return <div className={`rounded-xl p-4 ${danger ? "bg-red-600 text-white" : "bg-slate-50 text-slate-900"}`}><div className="text-sm opacity-80">{label}</div><div className="mt-1 text-2xl font-bold">{value}</div></div>; }
